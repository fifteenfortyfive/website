<nav class="navbar">
  <div class="container">
    <div class="navbar-brand">
      <a class="navbar-item navbar-brand-icon" href="/">
        <span class="brand-logo">The 1545</span>
      </a>
      <span class="navbar-burger burger" data-target="navbarMenuHeroA">
        <span></span>
        <span></span>
        <span></span>
      </span>
    </div>

    <div class="navbar-menu">
      <div class="navbar-end">
        {% if conn.user %}
          <div class="navbar-item has-dropdown is-hoverable">
            <div class="navbar-link">
              {% if conn.user.avatar_object_id %}
                <img class="avatar avatar--navbar" src="{{ conn.constants.assets_url }}/{{ conn.user.avatar_object_id }}">
              {% endif %}
              {{ conn.user.username }}
            </div>

            <div class="navbar-dropdown is-boxed">
              <a class="navbar-item">Profile</a>
              <a class="navbar-item" href="/accounts/edit">Edit Account</a>
              <div class="navbar-divider"></div>
              <a class="navbar-item" href="/signout">Log Out</a>
            </div>
          </div>
        {% else %}
          <p class="navbar-item">
            <a class="button is-danger is-outlined" href="/signin">
              Sign In
            </a>
          </p>
        {% endif %}

        <div class="navbar-item is-divider">
        </div>

        <div class="navbar-item">
          <div class="buttons are-large">
            <a class="button is-white" href="{{conn.constants.discord_url}}" target="_blank">
              <i class="fab fa-discord"></i>
            </a>
            <a class="button is-white" href="{{conn.constants.srcom_url}}" target="_blank">
              <i class="fa fa-trophy"></i>
            </a>
            <a class="button is-white" href="{{conn.constants.twitch_url}}" target="_blank">
              <i class="fab fa-twitch"></i>
            </a>
            <a class="button is-white" href="{{conn.constants.twitter_url}}" target="_blank">
              <i class="fab fa-twitter"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
